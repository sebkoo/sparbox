# BYOK: real keys are entered in the app and stored via expo-secure-store at
# runtime. They never live in files, this repo, or the JS bundle.
EXPO_PUBLIC_LLM_PROVIDER=
# EXPO_PUBLIC_IAP_ENABLED=false   (flag lands with the paywall stub commit)
